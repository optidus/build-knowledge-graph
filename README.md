# build-knowledge-graph
Automatically convert your existing BigQuery tables into a Property Graph — no manual schema design required.  It uses Gemini to analyze your table schemas, identify the right nodes and edges, generate the graph DDL, and then produce a set of sample GQL questions you can ask immediately.
